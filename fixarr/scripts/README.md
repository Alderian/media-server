# Helper Scripts

This directory contains third-party or custom scripts used by the main `organize_media.sh` orchestrator.

For example, it can contain the `bash-media-manager` scripts for organizing movies and TV shows.
